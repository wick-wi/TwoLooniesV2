# Plaid (required for Connect Bank flow)
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret

# Supabase (required for Save & Create Account)
SUPABASE_URL=https://your-project.supabase.co
# Use the new secret key (sb_secret_...) from Project Settings → API → Secret keys
# Legacy: SUPABASE_SERVICE_KEY also works as fallback
SUPABASE_SECRET_KEY=your_secret_key
SUPABASE_JWT_SECRET=your_jwt_secret
